{
  "name": "web-content-extractor",
  "version": "0.1.0",
  "description": "Browser UI and Playwright helper for extracting web content for LLM workflows.",
  "scripts": {
    "start": "node extract-server.js",
    "playwright:install": "playwright install --with-deps"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.1.0",
    "playwright": "^1.56.1"
  }
}
